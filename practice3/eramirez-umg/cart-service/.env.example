PORT=3000
REDIS_HOST=localhost
REDIS_PORT=6379
CART_TTL_SECONDS=86400
CATALOG_SERVICE_URL=http://localhost:8080
FRONTEND_ORIGIN=http://localhost:4200,http://localhost:81
# Must match the existing .NET JWT configuration; never commit the real key.
JWT_KEY=
JWT_ISSUER=EnterpriseApi
JWT_AUDIENCE=EnterpriseApp
